{% extends 'BigfootContentBundle:templates/page:layout.html.twig' %}

{% block page_content %}
    <article>
        {% if page.title %}<h1>{{ page.title }}</h1>{% endif %}
        <div class="richText">
            {{ page.description|raw }}
        </div>
    </article>
{% endblock page_content %}
